---
controller_notifications:
  - name: "network-events slack channel"
    organization: "Default"
    notification_type: "slack"
    notification_configuration:
      token: "{{ notification_secrets.network-events_slack_channel.token }}"
      token: "{{ slack_token }}"
      channels: ['#network-events']
