---
ansible-navigator:
  ansible:
    config:
      path: ansible.cfg
  execution-environment:
    image: aap.rh.lab/ee-aap-cac:2.0
    pull:
      policy: missing
  playbook-artifact:
    enable: false
